Follow `/.ai/START-HERE.md` for all shared conventions, vocabulary, and rules — that file is this
repo's full protocol, kept in one place so every AI tool (Claude Code, Kiro, Antigravity) reads the
same thing.

Your area: check `.ai/onboarding/team-brief.md` for the current slice ownership. Don't edit shared
core folders (`apps/api/src`, the data model, `packages/shared`) — ask first.
