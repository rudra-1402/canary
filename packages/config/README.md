# @canary/config

Shared ESLint/Prettier/Tailwind presets for `apps/web` and `apps/api`.

Currently an empty scaffold — this package exists so the workspace wiring is proven end-to-end.
Real lint/format configs are added in a follow-up pass (the format/lint gates stage).
