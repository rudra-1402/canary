// Cross-app contracts (Zod schemas) shared between apps/web and apps/api.
// Intentionally empty — real contracts land in a follow-up pass once the
// first API shape is agreed (see the Trust Score / API-contract design work).
export {};
